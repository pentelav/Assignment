// JavaScript: Calculate the sum of an array with syntax errors
function calculateSum(arr) {
    let total = o;  // Error: 'o' should be '0'
    for (let num of arr) {
        total += num;
    }
    return total;
}

let numbers = [1, 2, 3, 4, 5];
let result = calculate Sum (numbers); // Error: Incorrect function name spacing
console.log("Sum in JavaScript:", result);
